{
	"requestContext":{
		"authorizer":{
			"user": "super.user@test.com"
		}
	},
	"pathParameters": {
		"account": "*"
	},
	"body": "{ search (search: {query: \"Testing Search Indexing\"}) { status { timems, rid }, facets, hits { found, start, hit { id, fields } } } }"
}
