{
  "private": true,
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": ["**/module-alias", "**/module-alias/**"]
  },
  "scripts": {
    "compile": "lerna run compile",
    "lint": "lerna run lint",
    "test-environment": "lerna run test-environment --stream",
    "test-entities": "lerna run test-entities --stream",
    "test-deleteblocks": "lerna run test-deleteblocks --stream",
    "test-unit-circle": "lerna run test-unit-circle --stream",
    "test-functional-connectivity": "lerna run test-functional-connectivity --stream",
    "test-functional-analytics-events": "lerna run test-functional-analytics-events --stream",
    "test-functional-analytics-queries": "lerna run test-functional-analytics-queries --stream",
    "test-integration-analytics": "lerna run test-integration-analytics --stream",
    "test-transactions": "lerna run test-transactions --stream",
    "test-productsetup": "lerna run test-productsetup --stream",
    "ts-test": "lerna run ts-test",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.8.1"
  }
}
