{
  "title": "ServerlessConfig",
  "type": "object",
  "definitions": {
    "lambda": {
      "properties": {
        "handler": {
          "type":"string"
        }
      },
      "required": ["handler"]
    }
  },
  "properties": {
    "provider": {
      "required": ["name", "runtime", "stage", "region", "profile", "environment"]
    },
    "functions": {
      "patternProperties": {
        ".+": { "$ref": "#/definitions/lambda" }
      }
    }
  },
  "required": ["service", "package", "provider", "plugins", "functions", "resources"]
}
