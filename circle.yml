# Technical Debt: AWS user is over-permissioned
# Technical Debt: AWS user needs cross account access
# Technical Debt: Add SES deployment
# Technical Debt: Add CloudSearch configuration

general:
  branches:
    only:
      - production
      - staging
      - development

machine:
  timezone:
    America/Los_Angeles
  node:
    version: 7.6.0
  environment:
    #SIX_VERBOSE: '2'

  services:
    #- redis
    #- elasticsearch
    # Add dynamoDB

dependencies:
  pre:
    - openssl aes-256-cbc -d -in ./deployment/config/aws.$CIRCLE_BRANCH.encrypted -k $SIX_SKELETON_KEY >> ~/.circlerc
    - git lfs pull
  post:
    - echo "$AWS_ACCOUNT $AWS_REGION $AWS_ACCESS_KEY_ID $AWS_SECRET_ACCESS_KEY"
    - serverless dynamodb install --stage $CIRCLE_BRANCH
    - tools/saveCommitHashInCircle.sh `node deployment/circle/read_last_commit.js`
    - source ~/.circlerc
    - echo $LAST_COMMIT
    - cat ~/.circlerc

    # Collect debug info
    #- npm ls
    #- ls node_modules -1
    #- df -h
    #- serverless --version

test:
  override:
    - npm run lint
    - npm run test-unit

deployment:
  production:
    branch: production
    commands:

      #Serverless (Lambdas, API Gateway, Roles)
      - serverless deploy --stage $CIRCLE_BRANCH

      ###########
      #EC2
      #- node ./deployment/ec2/destroy_security_groups.js
      - node ./deployment/ec2/deploy_security_groups.js
      ###########

      ###########
      #S3
      #- node ./deployment/s3/destroy_buckets.js
      - ./tools/changesInDirectory.sh deployment/s3 && node ./deployment/s3/deploy_buckets.js || echo "No changes, skipping."
      ###########

      ###########
      #SQS
      - ./tools/changesInDirectory.sh deployment/sqs && node ./deployment/sqs/deploy_queues.js || echo "No changes, skipping."
      #- node ./deployment/sqs/purge_queues.js
      ###########

      ###########
      #CloudSearch
      #- ./tools/inCommit.sh ^deployment/cloudsearch/.* && echo "skipping, no updates" || node ./deployment/cloudsearch/deploy.js
      - ./tools/changesInDirectory.sh deployment/cloudsearch &&  node ./deployment/cloudsearch/deploy.js || echo "No changes, skipping."
      #- node ./deployment/cloudsearch/purge.js

      ###########

      ###########
      #ElastiCache
      #- ./tools/inCommit.sh ^deployment/elasticache/.* && echo "skipping, no updates" || node ./deployment/elasticache/deploy.js
      - ./tools/changesInDirectory.sh deployment/elasticache &&  node ./deployment/elasticache/deploy.js || echo "No changes, skipping."
      #- node ./deployment/elasticache/purge.js
      ###########

      ###########
      #DynamoDB
      # Technical Debt: Upgrade this to purge only TEST data
      #- node ./deployment/dynamodb/purge_tables.js
      - ./tools/changesInDirectory.sh deployment/dynamodb &&  node ./deployment/dynamodb/deploy_tables.js || echo "No changes, skipping."
      ###########

      ##########
      #Redshift
      - ./tools/changesInDirectory.sh deployment/redshift &&  node ./deployment/redshift/deploy_cluster.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/redshift &&  ./deployment/redshift/deploy_tables.js || echo "No changes, skipping."

      #Technical Debt:  Eliminate this.
      - ./tools/changesInDirectory.sh deployment/redshift && echo "Pausing to allow queries to complete..." && sleep 240 || echo "No changes, skipping."

      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/seed_referential.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/seed_tables.js || echo "No changes, skipping."
      ###########

      ###########
      #Kinesis
      - ./tools/changesInDirectory.sh deployment/kinesis &&  node ./deployment/kinesis/deploy_streams.js || echo "No changes, skipping."
      ###########

      ###########
      #Dynamo Seeds
      - node ./deployment/dynamodb/deploy_seeds.js
      ###########

      - SIX_VERBOSE=2 node deployment/circle/save_last_commit.js

      - export SIX_VERBOSE=0 && npm run test-transactions
      - export SIX_VERBOSE=0 && npm run test-graph
      - export SIX_VERBOSE=0 && npm run test-analytics

  staging:
    branch: staging
    commands:

      #Serverless (Lambdas, API Gateway, Roles)
      - serverless deploy --stage $CIRCLE_BRANCH

      ###########
      #EC2
      #- node ./deployment/ec2/destroy_security_groups.js
      - node ./deployment/ec2/deploy_security_groups.js
      ###########

      ###########
      #S3
      #- node ./deployment/s3/destroy_buckets.js
      - ./tools/changesInDirectory.sh deployment/s3 && node ./deployment/s3/deploy_buckets.js || echo "No changes, skipping."
      ###########

      ###########
      #SQS
      - ./tools/changesInDirectory.sh deployment/sqs && node ./deployment/sqs/deploy_queues.js || echo "No changes, skipping."
      #- node ./deployment/sqs/purge_queues.js
      ###########

      ###########
      #CloudSearch
      - ./tools/changesInDirectory.sh deployment/cloudsearch && node ./deployment/cloudsearch/deploy.js || echo "No changes, skipping."
      #- node ./deployment/cloudsearch/purge.js
      ###########

      ###########
      #ElastiCache
      - ./tools/changesInDirectory.sh deployment/elasticache && node ./deployment/elasticache/deploy.js || echo "No changes, skipping."
      #- node ./deployment/elasticache/purge.js
      ###########

      ###########
      #DynamoDB
      # Technical Debt: Upgrade this to purge only TEST data
      #- node ./deployment/dynamodb/purge_tables.js
      - ./tools/changesInDirectory.sh deployment/dynamodb && node ./deployment/dynamodb/deploy_tables.js || echo "No changes, skipping."
      ###########

      ##########
      #Redshift
      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/deploy_cluster.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/deploy_tables.js || echo "No changes, skipping."

      #Technical Debt:  Eliminate this.
      - ./tools/changesInDirectory.sh deployment/redshift && echo "Pausing to allow queries to complete..." && sleep 240 || echo "No changes, skipping."

      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/seed_referential.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/seed_tables.js || echo "No changes, skipping."
      ###########

      ###########
      #Kinesis
      - ./tools/changesInDirectory.sh deployment/kinesis && node ./deployment/kinesis/deploy_streams.js || echo "No changes, skipping."
      ###########

      ###########
      #Dynamo Seeds
      - ./tools/changesInDirectory.sh deployment/dynamodb && node ./deployment/dynamodb/deploy_seeds.js || echo "No changes, skipping."
      ###########

      - SIX_VERBOSE=2 node deployment/circle/save_last_commit.js

      - export SIX_VERBOSE=0 && npm run test-transactions
      - export SIX_VERBOSE=0 && npm run test-graph
      - export SIX_VERBOSE=0 && npm run test-analytics

      # Merge to upper environment
      - ./deployment/utilities/merge_branches.sh staging production

  development:
    branch: development
    commands:

      #Serverless (Lambdas, API Gateway, Roles)
      - serverless deploy --stage $CIRCLE_BRANCH

      ###########
      #EC2
      #- node ./deployment/ec2/destroy_security_groups.js
      - node ./deployment/ec2/deploy_security_groups.js
      ###########

      ###########
      #S3
      #- node ./deployment/s3/destroy_buckets.js
      - ./tools/changesInDirectory.sh deployment/s3 && node ./deployment/s3/deploy_buckets.js || echo "No changes, skipping."
      ###########

      ###########
      #SQS
      - ./tools/changesInDirectory.sh deployment/sqs && node ./deployment/sqs/deploy_queues.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/sqs && node ./deployment/sqs/purge_queues.js || echo "No changes, skipping."
      ###########

      ###########
      #CloudSearch
      - ./tools/changesInDirectory.sh deployment/cloudsearch && node ./deployment/cloudsearch/deploy.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/cloudsearch && node ./deployment/cloudsearch/purge.js || echo "No changes, skipping."
      ###########

      ###########
      #ElastiCache
      - ./tools/changesInDirectory.sh deployment/elasticache && node ./deployment/elasticache/deploy.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/elasticache && node ./deployment/elasticache/purge.js || echo "No changes, skipping."
      ###########

      ###########
      #DynamoDB
      #- node ./deployment/dynamodb/purge_tables.js && exit 0;
      - ./tools/changesInDirectory.sh deployment/dynamodb && node ./deployment/dynamodb/deploy_tables.js || echo "No changes, skipping."
      ###########

      ###########
      #IAM roles
      - node ./deployment/iam/destroy_roles.js
      - node ./deployment/iam/deploy_roles.js
      ###########

      ###########
      #Redshift
      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/deploy_cluster.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/deploy_tables.js && echo "Pausing to allow queries to complete..." && sleep 240 || echo "No changes, skipping."

      #Technical Debt:  Eliminate this.
            - ./tools/changesInDirectory.sh deployment/redshift && echo "Pausing to allow queries to complete..." && sleep 240 || echo "No changes, skipping."

      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/seed_referential.js || echo "No changes, skipping."
      - ./tools/changesInDirectory.sh deployment/redshift && node ./deployment/redshift/seed_tables.js || echo "No changes, skipping."
      ###########

      ###########
      #Kinesis
      - ./tools/changesInDirectory.sh deployment/kinesis && node ./deployment/kinesis/deploy_streams.js || echo "No changes, skipping."
      ###########

      ###########
      #Dynamo Seed
      - ./tools/changesInDirectory.sh deployment/dynamodb && node ./deployment/dynamodb/deploy_seeds.js || echo "No changes, skipping."
      ###########

      - SIX_VERBOSE=2 node deployment/circle/save_last_commit.js

      - export SIX_VERBOSE=0 && npm run test-transactions
      - export SIX_VERBOSE=0 && npm run test-graph
      - export SIX_VERBOSE=0 && npm run test-analytics


      # Merge to upper environment
      - ./deployment/utilities/merge_branches.sh development staging
