-- campaigns, product, product_schedule, affiliate, subids 1-5, mid (merchant provider id)
SELECT
  DISTINCT affiliate
FROM
  f_events
WHERE
  datetime BETWEEN '2018-01-01 00:00:00' AND '2019-01-01 00:00:00') affiliates
