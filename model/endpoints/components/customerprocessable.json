{
	"$schema": "http://json-schema.org/draft-04/schema",
	"id":"/endpoints/components/customerprocessable.json",
	"title": "Processable Customer",
	"description": "Customer with all fields required to process an order",
	"type": "object",
	"allOf": [
		{ "$ref": "../../entities/customer.json" },
		{
			"required":["email", "firstname", "lastname", "address"]
		}
	]
}
