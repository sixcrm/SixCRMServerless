{
	"requestContext":"{\"authorizer\":{\"user\":\"super.user@test.com\"}}",
	"pathParameters": "{ \"account\": \"*\" }",
	"body":"
	query {
		notificationlist ({argumentation}) {
			notifications {
		    id,
		    type,
		    action,
		    message,
		    read_at,
				created_at,
				updated_at
			},
      pagination {
          count,
          end_cursor,
          has_next_page,
					last_evaluated
      }
		}
	}"
}
