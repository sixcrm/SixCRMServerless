{
	"requestContext":"{\"authorizer\":{\"user\":\"super.user@test.com\"}}",
	"pathParameters": "{ \"account\": \"*\" }",
	"body":"
	query {
		transactionlist ({argumentation}) {
			transactions {
				id,
				amount,
				created_at,
				updated_at,
        merchant_provider {
            id,
            name,
            username,
            password,
            endpoint,
            processor,
            created_at,
            updated_at
        },
				rebill {
					id,
					bill_at,
					amount,
					parentsession {
						id,
						customer {
							id,
							firstname,
							lastname,
							address {
								line1,
								line2,
								city,
								state,
								zip
							}
						}
					},
					product_schedules {
						id,
						schedule {
							price,
							start,
							end,
							period,
							product {
								id
							}
						}
					}
				},
				processor_response,
				products {
					amount,
					product {
						id,
						name,
						sku,
						ship,
						shipping_delay,
						fulfillment_provider {
							id,
							name,
							provider,
							username,
							password,
							endpoint
						}
					},
					shippingreceipt {
						id,
						created_at,
						updated_at,
						status,
						trackingnumber
					}
				}
			},
			pagination {
				count,
				end_cursor,
				has_next_page
			}
		},
	}"
}
