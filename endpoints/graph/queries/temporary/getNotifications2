{
	"requestContext":"{\"authorizer\":{\"user\":\"test@test.com\"}}",
	"pathParameters": "{ \"account\": \"*\" }",
	"body":"
	query {
    notificationlist(pagination:{limit:\"2\"}) {
        notifications {
            id,
            type,
            action,
            message,
            read_at,
            created_at,
            updated_at
        },
        pagination {
            count,
            end_cursor,
            has_next_page,
            last_evaluated
        }
    }
	}"
}
